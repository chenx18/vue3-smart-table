# npm 配置文件

# 注册表配置（默认使用官方 npm 源）
registry=https://registry.npmjs.org/

# 如果使用淘宝镜像源，可以取消下面的注释
# registry=https://registry.npmmirror.com

# 发布配置
access=public

# 引擎检查
engine-strict=true

# 安装选项
save-exact=false
save-prefix=^

# 其他配置
@vue3-smart-table:registry=https://registry.npmjs.org/
